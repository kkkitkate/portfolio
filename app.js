document.querySelectorAll('[data-site]').forEach(el=>{const k=el.dataset.site;if(SITE[k])el.textContent=SITE[k]});
const heroCover=document.querySelector('#hero-cover'),heroCaption=document.querySelector('#hero-caption');if(heroCover&&FEATURED.length){const p=FEATURED[Math.floor(Math.random()*FEATURED.length)];heroCover.src=p.cover;heroCover.alt=`${p.title} — ${p.theatre}`;if(heroCaption)heroCaption.textContent=`${p.title} · ${p.theatre} · ${p.year}`}
const grid=document.querySelector('#featured-grid');if(grid){FEATURED.forEach(p=>{const e=document.createElement('article');e.className='project';e.innerHTML=`<img src="${p.cover}" alt="${p.title}"><div class="project-info"><h3>${p.title}</h3><div class="meta">${p.theatre}<br>${p.director?'режиссер '+p.director+'<br>':''}${p.year}</div><a class="btn" href="${p.internal||p.external||'#'}">Подробнее <span>→</span></a></div>`;grid.appendChild(e)})}
const table=document.querySelector('#projects-table');if(table){PROJECTS.forEach(p=>{const internal=!!p.internal,href=p.internal||p.external||'#',target=(!internal&&p.external)?' target="_blank" rel="noopener"':'',arrow=internal?'→':(p.external?'↗':'');let c=[];if(p.director)c.push(`режиссер ${p.director}`);if(p.designer)c.push(`художник-постановщик ${p.designer}`);if(p.extraCredit)c.push(p.extraCredit);const r=document.createElement('div');r.className='projects-row';r.innerHTML=`<div class="year">${p.year}</div><div><a class="project-name" href="${href}"${target}>${p.title} <span class="arrowmark">${arrow}</span></a></div><div class="credits-col">${c.join('<br>')||'—'}</div><div class="venue">${p.theatre}</div>`;table.appendChild(r)})}
